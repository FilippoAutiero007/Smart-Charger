import { ConvexReactClient } from "convex/react";
import Constants from "expo-constants";

const convexUrl = "https://striped-shrimp-908.convex.cloud";

export const convex = new ConvexReactClient(convexUrl);
